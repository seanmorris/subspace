version: '3'
services:

  is.seanmorr.subspace-terminal.web:
    build:
      context: ../
      dockerfile: infra/app.Dockerfile
    image: seanmorris/subspace-terminal-web
    restart: always
    ports:
      - "80:80"
    volumes:
      - ../:/app
      # - ../data/local/certbot:/etc/letsencrypt/live/subspace2.seanmorr.is/
    depends_on:
      - is.seanmorr.subspace-terminal.database

  is.seanmorr.subspace-terminal.socket:
    build:
      context: ../
      dockerfile: infra/socket.Dockerfile
    image: seanmorris/subspace-terminal-socket
    restart: always
    ports:
      - "9998:9998"
    volumes:
      - ../:/app
    depends_on:
      - is.seanmorr.subspace-terminal.database

  is.seanmorr.subspace-terminal.database:
    image: mysql:5.7
    restart: always
    ports:
      - "9896:3306"
    volumes:
      - is.seanmorr.subspace-terminal.schema:/var/lib/mysql
    environment:
      MYSQL_USER:          'subspace-terminal'
      MYSQL_DATABASE:      'subspace-terminal'
      MYSQL_PASSWORD:      'password'
      MYSQL_ROOT_PASSWORD: 'supersecret'
    command: ['mysqld', '--character-set-server=utf8mb4']

  is.seanmorr.subspace-terminal.worker:
    build:
      context: ../
      dockerfile: infra/worker.Dockerfile
    image: seanmorris/subspace-terminal-worker
    restart: on-failure
    volumes:
      - ../:/app
    depends_on:
      - is.seanmorr.subspace-terminal.database

  # is.seanmorr.subspace-terminal.updater:
  #   build:
  #     context: ../
  #     dockerfile: infra/updater.Dockerfile
  #   image: seanmorris/subspace-terminal-updater
  #   restart: on-failure
  #   volumes:
  #     - ../:/app
  #   depends_on:
  #     - is.seanmorr.subspace-terminal.database

  is.seanmorr.subspace-terminal.watcher:
    build:
      context: ../
      dockerfile: infra/watcher.Dockerfile
    image: seanmorris/subspace-terminal-watcher
    ports:
      - "9485:9485"
      - "3333:3333"
    volumes:
      - ../:/app

  is.seanmorr.subspace-terminal.rabbit:
    image: "rabbitmq:3-management"
    restart: always
    environment:
      RABBITMQ_ERLANG_COOKIE: "O234234QALRPCLNMEQG"
      RABBITMQ_DEFAULT_USER: "rabbitmq"
      RABBITMQ_DEFAULT_PASS: "rabbitmq"
      RABBITMQ_DEFAULT_VHOST: "/"
    ports:
      - "19994:15672"
      - "9994:5672"
    volumes:
      - "../data/global/rabbitMq/enabled_plugins:/etc/rabbitmq/enabled_plugins"

  # is.seanmorr.subspace-terminal.composer:
  #   build:
  #     context: ../
  #     dockerfile: infra/composer.Dockerfile
  #   image: seanmorris/subspace-terminal-composer
  #   restart: on-failure
  #   volumes:
  #     - ../:/app
  #     - ../data/local/composer/:/root/.composer/

  # is.seanmorr.subspace-terminal.certbot:
  #   image:   certbot/certbot
  #   command: renew
  #   volumes:
  #     - ../data/local/certbot:/etc/letsencrypt/live/

  # is.seanmorr.subspace-terminal.fakecert:
  #   image: vishnunair/docker-mkcert:latest
  #   environment:
  #     domain: localhost
  #   volumes:
  #     - ../data/local/certbot/subspace2.seanmorr.is:/root/.local/share/mkcert

volumes:
  is.seanmorr.subspace-terminal.schema:
    driver: "local"
